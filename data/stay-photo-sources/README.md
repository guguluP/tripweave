# Stay photo sources

Official hotel CDN URLs used by `npm run materialize:stays` to populate `public/stays/`.

- `index.json` — referers, proxy_hosts, stay id list
- `{stayId}.json` — gallery + room url/path map for that stay
