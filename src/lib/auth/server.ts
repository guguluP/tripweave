/**
 * Self-hosted Better Auth for THIS app (server-only).
 *
 * Pre-wired for live preview + deploy — do not rewrite this file. To enable
 * local email/password, flip the flag in `./email-password` only (see auth skill).
 *
 * SEE REPO HISTORY COMMIT 02a421b FOR FULL FILE IF THIS IS TRUNCATED.
 * Run on Mac:
 *   git checkout 02a421b -- src/lib/auth/server.ts src/routes/login.tsx
 */
export { GROK_PROVIDERS } from "./providers";
throw new Error("server.ts was corrupted — run: git checkout 02a421b -- src/lib/auth/server.ts src/routes/login.tsx");
