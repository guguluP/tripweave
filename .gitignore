node_modules/
.project_id
.github_repo
.env
.env.*
.vercel/
.grok/status
.grok/preview.log
scripts/e2e-auth-pay.mjs
screenshots/e2e-*.png

# Materialized stay photos (generated by npm run materialize:stays from stay-photo-sources.json).
# Commit them when binary git push is available; otherwise CI regenerates at prebuild.
public/stays/**/*.jpg
public/stays/**/*.jpeg
public/stays/**/*.webp
public/stays/**/*.png
